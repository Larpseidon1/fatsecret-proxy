{
  "name": "fatsecret-proxy",
  "version": "1.0.0",
  "description": "Proxy server for FatSecret API to handle OAuth and IP whitelisting",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "node-fetch": "^2.7.0"
  },
  "devDependencies": {
    "@railway/cli": "^4.11.1",
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
